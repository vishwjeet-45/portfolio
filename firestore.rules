rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public site content: anyone can read, only a signed-in admin can write.
    match /site/content {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
