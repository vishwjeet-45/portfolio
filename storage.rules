rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Uploaded images (hero photo, testimonials): public read, admin-only write.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
